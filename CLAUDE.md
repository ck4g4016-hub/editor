# 給下一個接手的人（含 AI）

這支程式在新北市政府稅捐稽徵處實際使用，處理的是民眾的個資。
下面幾條不是建議，是**規矩**。

---

## 一、改完一定要自己跑過，再交出去

承辦人 2026-09-09 明確講的：「以後如果有任何改動，先自己跑看看有沒有問題。
多燒點 token 也沒關係，不然這樣太笨了。」

這條是有代價才學到的。2026-09-08 我為了補資安漏洞替本機伺服器加上權杖，
把所有 `fetch()` 都包進 `api()`，卻漏掉 `editor/page.html` 裡用 `img.src`
載入掃描影像的那一行。結果：**樣板編輯器左邊整片空白，而且畫面上沒有任何
錯誤訊息**。承辦人看到的是「以前能跑出來的東西全部都無法顯示」。

當時我跑了單元檢查而且全過 —— 因為那些檢查測的是「防護擋不擋得住惡意
請求」，沒有一條測「正常使用還能不能用」。**測試綠燈不等於東西能用。**

### 交出去之前，至少要做到

1. `python3 tools/selftest.py` 要過。**這是下限，不是上限。**
2. **動到哪一塊，就把那一塊真的跑起來看**：

   | 改到 | 至少要驗 |
   | --- | --- |
   | `editor/*.html`、`tools/template_editor.py`、`tools/review.py` | 起一台伺服器，把畫面會發的**每一個**請求打一遍，確認回的是預期的內容（不是只看狀態碼 200） |
   | `pipeline/output.py` | 產出檔案，**讀回來**逐格比對 |
   | `pipeline/pagetext.py`、`recognise.py`、`validate.py`、`lexicon.py` | 拿真實的樣本 PDF 跑，把每一欄的結果印出來看 |
   | `.github/workflows/build-app.yml` | 至少 `yaml.safe_load` 解析過；能等就等建置跑完 |
   | 資安相關 | 正向與負向都要：該擋的擋、**該過的也要過** |

3. **加了新檢查，就做一次負向驗證** —— 故意把東西弄壞，確認檢查真的會叫。
   不會叫的檢查比沒有檢查更糟，它給的是假的安心。

### 不要做的事

* 不要因為「這只是一行」就跳過驗證。上面那個 bug 就是一行。
* 不要用「測試都過了」代替「我看過它能用」。
* 不要把沒跑過的東西講成「修好了」。沒驗證就說沒驗證。

---

## 二、這支程式必須完全離線

處理的是民眾個資，**不能有任何一個位元流到機關外面**。

`tools/selftest.py` 有兩層在守：靜態用 AST 掃出貨的程式碼不得出現
`socket` / `urllib.request` / `requests` 這類模組；動態攔截
`socket.connect` 與 `getaddrinfo`，載入模型跑一次真實辨識，零連線。

**打包前這個檢查沒過就不會出檔。** 不要為了方便繞過它。

---

## 三、correct-and-flag，永遠不要 correct-silently

門牌、身分證號、公文文號這三欄錯了，RPA 會拿著錯的資料去查別人的房子，
**而且從輸出的 Excel 上完全看不出來**。

所以：值可以修（多半是對的，省得人重打），但**只要動到讀出來的內容，
一定要在複核畫面上標記**。`pipeline/lexicon.py` 的 `resolve_head_full`
在出口統一補這個標記，就是因為比對有四條路徑，每加一條就多一個忘記標記
的機會。

分不出來的時候就回報分不出來。**不要猜。**

---

## 四、跟外部系統的介接：名字和格式是別人定的

* 內網中繼檔的標題列（`序號`／`行政區`／`所有權人IDN`／`完整地址`／`姓名`）
  是內網腳本**用名稱取值**的依據，一個字都不能改。
* 戶政清冊 `YHQ101_民國年月日.xls` 的規格見 `docs/output-spec.md`，
  來自戶政系統的範例檔，逐格核對過。
* 這些改動之前先讀 `docs/output-spec.md`，那裡記著每一條規則**為什麼**
  長這樣、以及踩過哪些坑。

---

## 五、寫給人看的東西用繁體中文

程式碼註解、提示訊息、錯誤訊息、說明文件，全部繁體中文。
註解要寫「為什麼」，不是「做了什麼」—— 做了什麼看程式碼就知道。
